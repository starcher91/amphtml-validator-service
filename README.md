# Amp HTML Validation Service

This service is a node web service that accepts a request, and when pinged, will go out and validate a given url for amp validity.

## Usage

* Install Docker and docker-compose
* Run `docker-compose up`